export type {
  ContentConcept,
  FrameworkConcept,
  FrameworkFamily,
  ConceptCheckResult,
} from "./types.js";
// Implementations land in stage 3 per spec 05_brain_and_learning.
// Skeleton lives here so downstream packages can import the types now.
